# Train-System
designing the conceptual and logical structure of the system via the Entity-Relationship Diagram (ERD) and the Relational Schema. This was followed by the transition to the actual implementation phase using SQL.
